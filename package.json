{
  "name": "personal-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate:writing": "node ./scripts/generate-writing.mjs",
    "predev": "npm run generate:writing",
    "dev": "vite",
    "prebuild": "npm run generate:writing",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^17.0.4"
  }
}
